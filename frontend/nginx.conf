server {
  listen 80;

  root /usr/share/nginx/html;
  index index.html;

  # Fix MIME type for .mjs files (needed for PDF.js worker)
  types {
    application/javascript js mjs;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }

  error_page 404 /index.html;
}
